---
# Stub — populated in a subsequent commit. Importing main.yml without this
# file would error out with `Could not find or access` on the import_tasks.
- name: Repo — stub
  ansible.builtin.debug:
    msg: "argus_stack/tasks/repo.yml is a stub; populated in the repo+install commit"
