{
  "name": "argus-recovery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Argus — recovery matrix automation (ralph cap, fake-completion guard, tmux restart, provider fallback, crash budget) for design §8 modes a-f",
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src tests",
    "format": "biome format --write src tests"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "pino-pretty": "^11.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
