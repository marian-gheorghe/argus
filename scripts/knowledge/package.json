{
  "name": "argus-knowledge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Argus — knowledge accumulation discipline (learner cadence, scope classifier, collision check, notepad summarizer) for design §9.",
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src tests",
    "format": "biome format --write src tests"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "pino-pretty": "^11.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
