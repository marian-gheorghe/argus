# Local secrets / runtime
.env
.env.local
*.local.toml
*.local.json
config/*.local.*

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node
node_modules/
npm-debug.log
yarn-error.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Bun
bun.lock

# Runtime artifacts (if anything ends up running from here)
*.log
*.pid
.cache/

# OMC / clawhip state if anyone accidentally runs from here
.omc/
.clawhip/

# Worktrees managed by OMC team mode (inside <repo>/.omc/team/.../worktrees)
worktrees/

# Argus dev worktrees (this repo's own feature branches)
.worktrees/

# Ansible — operator-filled inventory and vaulted secrets.
# Only the *.example files are committed.
ansible/inventory/hosts.yml
ansible/inventory/group_vars/argus_vps.yml
ansible/inventory/group_vars/argus_vps.vault.yml
ansible/collections/
ansible/.cache/
ansible/*.retry
